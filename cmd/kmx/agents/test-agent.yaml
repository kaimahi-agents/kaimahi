# Generated by `kmx agent create test-agent`.
# This file IS the agent: edit it, review it, commit it, re-apply it.
# kmx adds no runtime — the kagent controller reconciles this document.
# This agent's model calls are UNGOVERNED: no budget, no ledger, no audit
# sits in front of them. `make plane` then `make govern` puts a governed
# preset in front of an agent (milestone 2 of kmx will own that seam).
apiVersion: kagent.dev/v1alpha2
kind: Agent
metadata:
  name: "test-agent"
  namespace: "kagent"
spec:
  description: "Agent test-agent, scaffolded by kmx."
  type: Declarative
  declarative:
    modelConfig: "hello-world-model"
    deployment:
      resources:
        requests:
          cpu: 50m
          memory: 320Mi
        limits:
          memory: 1Gi
    systemMessage: |2
      You are test-agent, a declarative kagent agent defined entirely in YAML and
      running on Kubernetes. Answer briefly and in plain text, and say so
      plainly when you do not know something.
